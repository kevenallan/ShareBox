import { LocalDateTimeFormatPipe } from './local-date-time-format.pipe';

describe('LocalDateTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalDateTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
